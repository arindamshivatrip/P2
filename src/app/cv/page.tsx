import { RoutePlaceholder } from "@/components/layout/route-placeholder";

export default function CvPage() {
  return (
    <RoutePlaceholder
      eyebrow="CV"
      title="CV route scaffold."
      description="Ready for embedded resume content and downloadable branch-specific artifacts."
    />
  );
}
